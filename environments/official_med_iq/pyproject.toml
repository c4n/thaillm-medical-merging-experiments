[project]
name = "official-med-iq"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.24.0",
    "verifiers>=0.1.12.dev6",
]

[tool.verifiers.eval]
num_examples = 5
rollouts_per_example = 3
